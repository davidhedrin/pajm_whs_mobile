import ScreenWrapper from '@/components/screen-wrapper'
import { CText } from '@/components/text'

const PurchaseRequest = () => {
  return (
    <ScreenWrapper edges={['bottom']}>
      <CText>PurchaseRequest</CText>
    </ScreenWrapper>
  )
}

export default PurchaseRequest